export const metadata = {
  title: "Books",
  description: "Concepts of Physics and other publications by H.C. Verma.",
};

export default function BooksLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
