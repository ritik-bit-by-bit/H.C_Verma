export const metadata = {
  title: "Lectures",
  description: "Upcoming and past lectures, workshops, and teaching sessions.",
};

export default function LecturesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
