export const metadata = {
  title: "Resources",
  description: "Downloads, useful links, and physics resources.",
};

export default function ResourcesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
